RegisterNetEvent('flake_drugsellingCL:notify', function(message, type)
    -- This event is triggered from client to show notifications
    -- The actual notification is handled on the client side via Config.Notify
end)

